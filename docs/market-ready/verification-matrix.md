# 绿动全城整体整改验证矩阵

更新日期：2026-07-28

## 一、自动验证结果

| 仓库 | 验证项 | 结果 | 说明 |
|---|---|---|---|
| `ldqc-boot` | BladeX 私有 Maven 仓库认证 | 通过 | GitHub Secret 临时生成 Maven settings，凭证不进入仓库 |
| `ldqc-boot` | 最新 JDK 17 Maven 编译 | 通过 | `mvn -B -U -DskipTests compile` |
| `ldqc-boot` | 评论审核重试与运营接口编译 | 通过 | 定时重试、人工处理、评论身份和菜单 SQL 已进入分支 |
| `ldqc-boot` | 推荐反馈排序接入编译 | 通过 | 行为权重、负反馈过滤和服务端身份已进入正式推荐 Service |
| `ldqc-vue` | 依赖安装 | 通过 | `yarn install --frozen-lockfile` |
| `ldqc-vue` | 管理端生产构建 | 通过 | `yarn build:prod` |
| `ldqc-wx` | `pages.json` 与路由目标 | 通过 | 全部注册页面对应 `.vue` 文件存在 |
| `ldqc-wx` | 生产接口配置 | 通过 | 生产配置不回落 localhost/127.0.0.1 |
| `ldqc-wx` | 商城旧直接兑换调用 | 通过 | 页面不再调用 `exchangeMallProduct` |
| `ldqc-wx` | 个人中心产品边界 | 通过 | 无好友、固定余额、相册组件；真实业务入口齐全 |
| `ldqc-wx` | 推荐反馈闭环 | 通过 | 曝光、点击、不感兴趣、详情停留和视频完成静态检查通过 |

## 二、代码闭环状态

| 模块 | 后端 | 管理端 | 小程序 | 自动验证 | 真实环境验收 |
|---|---|---|---|---|---|
| 社区发布与审核 | 发布者身份、状态机、媒体处理完成 | 内容审核工作台完成 | 发布、进度和状态页完成 | 后端编译、管理端构建通过 | 待对象存储、审核服务与真机 |
| 评论内容安全 | 服务端身份、审核任务、退避重试、人工处理完成 | 审核异常工作台完成 | 新闻和达人详情使用真实评论接口 | 后端编译、管理端构建通过 | 待微信审核接口和测试库定时任务 |
| 达人与关注 | 聚合、发布过滤、关注计数完成 | 内容与课程运营入口完成 | 主页、作品、教程、课程、关注和粉丝完成 | 三端自动验证通过 | 待多账号联调 |
| 长视频课程 | 授权、进度、媒体处理完成 | 课程与内容工作台完成 | 目录、试看、锁定和续播完成 | 后端编译、管理端构建通过 | 待 FFmpeg、存储和弱网真机 |
| 赛事报名 | 名额、快照、取消、免费/付费边界完成 | 赛事管理继续使用 | 真实报名和订单页面完成 | 三端自动验证通过 | 付费赛事待微信支付 |
| 绿动有约 | 人数、隐私、发布人审核、管理员代审和日志完成 | 全局待办、详情和申请处理工作台完成 | 发布、申请、审核和参与名单完成 | 三端自动验证通过 | 待多角色与并发联调 |
| 场馆入驻 | 申请、审核、创建和运营者绑定完成 | 入驻审核工作台完成 | 申请、记录和场馆工作台完成 | 三端自动验证通过 | 待证照存储和角色联调 |
| 场馆在线预约 | 延期 | 延期 | 不展示误导性订场入口 | 边界文档完成 | 独立阶段确认规则后开发 |
| 优惠券 | 领取、核销、范围、库存和权限收口完成 | 模板与核销运营字段完成 | 领取、动态码和记录适配完成 | 三端自动验证通过 | 待测试库全流程和并发 |
| 商城兑换 | 确认、快照、绿豆库存事务和履约状态机完成 | 商品与履约工作台完成 | 详情、确认、订单和履约信息完成 | 三端自动验证通过 | 待库存/绿豆/退款并发验证 |
| 推荐反馈 | 模型、幂等、排序权重、负反馈和登录身份完成 | 暂无独立运营页 | 曝光、点击、不感兴趣、停留和视频完成完成 | 后端编译、小程序检查通过 | 待真实数据效果和行为分析 |
| 个人中心 | 服务端资产与身份接口已收口 | 不适用 | 已重构为真实业务入口 | 小程序边界检查通过 | 待微信开发者工具和真机 |

## 三、SQL 执行前置条件

1. 使用 MySQL 5.7 测试库完整备份后执行。
2. 按 `docs/market-ready` 与 `doc/sql` 中的执行顺序逐份执行。
3. 结构迁移、菜单权限、历史数据修复和 UAT 数据必须分开执行。
4. 评论审核重试新增 `doc/sql/content_audit_retry_upgrade.sql`，执行后检查状态 3/4、重试索引和历史任务初始化。
5. 每份 SQL 执行后检查字段、唯一索引、历史状态和可重复执行性。
6. 未在测试库执行成功前，不得进入生产库。

## 四、仍需真实环境完成

- MySQL 5.7 全量迁移与回滚演练。
- MinIO/OSS 图片、视频、证照和商品图上传。
- 微信内容安全审核成功、拒绝、超时、重试耗尽和人工处理测试。
- FFmpeg/FFprobe 长视频封面与时长处理。
- 微信开发者工具编译、真机和弱网测试。
- 普通用户、达人、场馆运营者、核销员、运营管理员多角色联调。
- 微信支付下单、回调、退款和对账；未接入前付费赛事保持阻断。
- 商城、优惠券、赛事和邀约的数据库并发验证。
- 性能目标、日志、告警和数据审计检查。

## 五、Draft PR 转为可审核的条件

1. 三端自动验证持续通过。
2. SQL 在测试库执行并形成记录。
3. P0/P1 主业务流程通过多角色联调。
4. 微信开发者工具和至少一轮真机回归完成。
5. 遗留问题明确分为上线阻断和后续迭代。
6. 由项目负责人确认后再转为 Ready for review，当前不自动合并 `master`。
