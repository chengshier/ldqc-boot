# 绿动全城整体整改进度记录（后端）

## 2026-07-27 第一批：基础契约与高风险入口收口

### 已完成

1. 建立唯一整改分支 `refactor/market-ready-phase1` 和 Draft PR，后续不再拆分阶段分支。
2. 绿动有约 Controller：
   - 管理端列表、新增、修改、删除、导出增加管理员边界；
   - 小程序取消与审核接口同时兼容 Query 参数和 JSON Body；
   - 接口说明改为面向业务流程的中文语义；
   - 缺少邀约 ID、申请 ID、审核动作时返回明确错误。
3. 优惠券核销：
   - 新增统一 `CouponVerificationService`；
   - 动态二维码、扫码预检、核销确认、记录查询统一走一套服务；
   - 严格按 `coupon_verifier_scope` 校验核销员范围；
   - 增加扫码会话和 Redis 互斥锁；
   - 校验券生效时间、过期时间、状态与模板；
   - 关闭旧 `/use` 直接核销旁路；
   - 用户券通用维护接口限制为管理员；
   - 用户只能查看自己的优惠券和核销记录。

### 当前待验证

- 尚未在真实数据库执行 `coupon_verifier_scope` 查询验证。
- 尚未运行 Maven 编译；需要确认 BladeX 私有依赖仓库在执行环境可用。
- 优惠券最终库存与券状态仍需通过并发联调验证。
- 绿动有约审核通过时的人数原子更新尚未完成，本批只先收口接口契约。

### 下一批后端范围

1. 内容发布者身份以后端登录用户为准；建立内容待审核、通过、拒绝、下架状态流。
2. 赛事报名：重复报名、名额、截止时间、免费/付费边界、真实订单状态。
3. 商城兑换：商品快照、库存与绿豆原子扣减、兑换订单和履约状态。
4. 长视频课程：章节、课时、媒体处理状态、试看和播放授权接口。
5. 绿动有约：审核并发控制、通过后联系方式与参与名单权限。

## 验收原则

未完成编译、SQL、接口联调和真机验证的内容，只记录为“代码已提交”，不标记为验收通过。
