# 后端整体优化 PR 审查指南

## 已完成并通过自动验证

- 使用 GitHub Secret 访问 BladeX 私有 Maven 仓库。
- JDK 17 执行 `mvn -B -U -DskipTests compile` 成功。
- 编译日志已保存为 GitHub Actions Artifact。
- 社区内容发布审核、达人与关注、长视频课程、赛事报名、绿动有约、场馆入驻、优惠券、商城兑换与推荐反馈已进入统一业务服务和权限边界。
- SQL、接口契约、状态说明和验证矩阵已放入 `docs/market-ready` 与 `doc/sql`。

## 审查重点

1. 用户身份是否始终由服务端登录态决定。
2. 内容、课程、赛事、优惠券、商城和场馆状态机是否存在绕过入口。
3. 库存、名额、绿豆、优惠券库存和关注计数是否使用原子更新或唯一约束。
4. 管理端动作接口是否与通用 CRUD 隔离。
5. 联系方式、身份证、手机号和播放地址是否按权限返回。
6. SQL 是否适配 MySQL 5.7，并区分结构、菜单、历史修复和测试数据。

## 合并前仍需完成

- MySQL 5.7 测试库迁移和回滚演练。
- 对象存储、FFmpeg/FFprobe 和长视频真实联调。
- 多角色接口联调和并发验证。
- 微信支付、回调、退款与对账；未完成前付费赛事保持阻断。
- 小程序微信开发者工具与真机回归。

当前 PR 保持 Draft，不自动合并 `master`。
